(()=>{
'use strict';
const VERSION='v221';
const CURRENT_ROSTER=['大久保 陽翔','大野 竜暉','井坂 悠聖','坂田 暉馬','嶋田 栄志','武澤 大翔','橋向 結都','中嶋 玲月','吉田 真翔','上村 蓮','大久保 夢翔','長侶 穹','鰐渕 将太','武田 晴琉翔'];
const A={name:['選手名','氏名','名前','選手'],pa:['打席数','打席','pa'],ab:['打数','ab'],h:['安打','h'],avg:['打率','avg'],obp:['出塁率','obp'],slg:['長打率','slg'],ops:['ops'],single:['単打'],double:['二塁打'],triple:['三塁打'],hr:['本塁打','hr'],rbi:['打点','rbi'],bb:['四球','bb'],hbp:['死球'],sf:['犠飛'],so:['三振','so','k']};
const relevant=q=>/クリーンナップ|中軸|主軸|打線|打順|打撃|打率|OPS|安打|三振|本塁打|打点/i.test(String(q||''));
const norm=s=>String(s??'').normalize('NFKC').replace(/[\s　・･_\-\/()（）\[\]【】]/g,'').toLowerCase();
const canonical=Object.fromEntries(CURRENT_ROSTER.map(x=>[norm(x),x]));
const cname=s=>canonical[norm(s)]||String(s||'').trim();
const idx=(r,a)=>{const cs=r?.columns||[],w=a.map(norm);for(let i=0;i<cs.length;i++)if(w.includes(norm(cs[i])))return i;for(let i=0;i<cs.length;i++)if(w.some(x=>x&&norm(cs[i]).includes(x)))return i;return-1};
const val=(r,a)=>{const i=idx(r,a);return i<0?'':String((r?.values||[])[i]??'').trim()};
const num=v=>{const s=String(v??'').replace(/,/g,'').trim();if(!s||s==='-'||s==='—')return null;const x=Number(s);return Number.isFinite(x)?x:null};
const rate=x=>Number.isFinite(x)?x.toFixed(3).replace(/^0(?=\.)/,''):'';
const isCurrent=r=>norm(`${r?.fileName||''} ${r?.sheetName||''}`).includes('20262027');
const isBatting=r=>/打撃|成績一覧|通算成績|打撃詳細|打率|OPS/i.test(`${r?.fileName||''} ${r?.sheetName||''} ${(r?.columns||[]).join(' ')}`);
const isDetail=r=>/打撃詳細/i.test(`${r?.fileName||''} ${r?.sheetName||''}`);
const isAggregate=r=>isCurrent(r)&&isBatting(r)&&!/打撃詳細/i.test(`${r?.fileName||''} ${r?.sheetName||''}`)&&/通算成績|成績一覧/i.test(`${r?.fileName||''} ${r?.sheetName||''}`);
const signature=r=>JSON.stringify([r?.columns||[],r?.values||[]]);
function dedupe(rows){const m=new Map();for(const r of rows||[]){const k=signature(r);if(!m.has(k))m.set(k,r)}return[...m.values()]}
function playerRows(rows,p,pred){const np=norm(p);return dedupe((rows||[]).filter(r=>isCurrent(r)&&isBatting(r)&&pred(r)&&norm(cname(val(r,A.name)))===np));}
function aggregateRecord(rows,p){const rr=playerRows(rows,p,isAggregate);if(!rr.length)return{record:null,conflicts:[]};const out={},conflicts=[];for(const k of ['pa','ab','h','avg','obp','slg','ops','hr','rbi','so']){const values=[...new Set(rr.map(r=>num(val(r,A[k]))).filter(x=>x!==null).map(x=>String(x)))];if(values.length>1)conflicts.push(`${p} ${k}：通算成績内で ${values.join(' / ')} が併存`);if(values.length)out[k]=Number(values[0]);}return{record:Object.keys(out).length?out:null,conflicts};}
function detailRecord(rows,p){const rr=playerRows(rows,p,isDetail);if(!rr.length)return null;const s={pa:0,ab:0,h:0,single:0,double:0,triple:0,hr:0,rbi:0,bb:0,hbp:0,sf:0,so:0},seen={};for(const r of rr){for(const k of Object.keys(s)){const x=num(val(r,A[k]));if(x!==null){s[k]+=x;seen[k]=true}}}let H=seen.h?s.h:null;if(H===null&&(seen.single||seen.double||seen.triple||seen.hr))H=s.single+s.double+s.triple+s.hr;const AB=seen.ab?s.ab:null,PA=seen.pa?s.pa:null,BB=seen.bb?s.bb:0,HBP=seen.hbp?s.hbp:0,SF=seen.sf?s.sf:0;const avg=AB>0&&H!==null?H/AB:null,obpd=(AB||0)+BB+HBP+SF,obp=obpd>0&&H!==null?(H+BB+HBP)/obpd:null;let singles=seen.single?s.single:(H!==null?Math.max(0,H-(seen.double?s.double:0)-(seen.triple?s.triple:0)-(seen.hr?s.hr:0)):null);const tb=AB>0&&singles!==null?singles+2*(seen.double?s.double:0)+3*(seen.triple?s.triple:0)+4*(seen.hr?s.hr:0):null,slg=AB>0&&tb!==null?tb/AB:null,ops=obp!==null&&slg!==null?obp+slg:null;return{pa:PA,ab:AB,h:H,avg,obp,slg,ops,hr:seen.hr?s.hr:null,rbi:seen.rbi?s.rbi:null,so:seen.so?s.so:null};}
function compare(p,a,d){const issues=[];if(!a||!d)return issues;for(const k of ['pa','ab','h','hr','rbi','so']){if(a[k]!==undefined&&a[k]!==null&&d[k]!==undefined&&d[k]!==null&&Number(a[k])!==Number(d[k]))issues.push(`${p} ${k}：通算 ${a[k]} / 試合別再集計 ${d[k]}`)}for(const k of ['avg','obp','slg','ops']){if(a[k]!==undefined&&a[k]!==null&&d[k]!==undefined&&d[k]!==null&&Math.abs(Number(a[k])-Number(d[k]))>0.0015)issues.push(`${p} ${k}：通算 ${rate(Number(a[k]))} / 試合別再集計 ${rate(Number(d[k]))}`)}return issues;}
function publicFact(m){return{season:'2026-2027',pa:m.pa??'',ab:m.ab??'',h:m.h??'',avg:rate(Number(m.avg)),obp:rate(Number(m.obp)),slg:rate(Number(m.slg)),ops:rate(Number(m.ops)),hr:m.hr??'',rbi:m.rbi??'',so:m.so??''}}
function build(rows){const facts={},conflicts=[],sources={};const aggregateExists=(rows||[]).some(isAggregate);for(const p of CURRENT_ROSTER){const ar=aggregateRecord(rows,p);conflicts.push(...ar.conflicts);const dr=detailRecord(rows,p);if(ar.record&&dr)conflicts.push(...compare(p,ar.record,dr));let chosen=null,source='';if(ar.record){chosen=ar.record;source=dr?'通算成績＋重複除去済み試合別再集計で照合':'通算成績'}else if(dr){chosen=dr;source='重複除去済み試合別再集計';if(aggregateExists)conflicts.push(`${p}：試合別データはあるが通算成績行を取得できない`)}if(chosen){facts[p]=publicFact(chosen);sources[p]=source}}
  if(!Object.keys(facts).length)conflicts.push('2026-2027の検証可能な打撃数値を取得できない');
  return{ok:conflicts.length===0,version:VERSION,conflicts:[...new Set(conflicts)],facts,sources,aggregateExists};
}
function stripOldSnapshot(text){let s=String(text||''),mark='【クリーンナップ候補・現チーム優先スナップショット】';const i=s.indexOf(mark);if(i<0)return s;const before=s.slice(0,i),after=s.slice(i+mark.length),j=after.indexOf('\n\n【');return(before+(j>=0?after.slice(j+2):'')).trim();}
function trustedBlock(integ){const entries=Object.entries(integ.facts).filter(([,m])=>m.ab!==''&&Number(m.ab)>0).sort((a,b)=>Number(b[1].ops||0)-Number(a[1].ops||0));const lines=entries.map(([p,m])=>`・${p}：${m.pa}打席 / ${m.ab}打数 / ${m.h}安打 / 打率${m.avg} / 出塁率${m.obp} / 長打率${m.slg} / OPS${m.ops} / 本塁打${m.hr} / 打点${m.rbi} / 三振${m.so}`);return`【NUMERIC SOURCE OF TRUTH / VERIFIED】\n2026-2027現チーム打撃数値。通算成績を正本とし、試合別CSVは完全重複行を除去して再集計し照合する。数値が不一致なら審議を開始しない。\n${lines.join('\n')}\n数値利用ルール：ここに明示された数値だけを現チームの数値事実として使用する。率だけでなく打席・打数・安打・三振などの母数も一致させる。`}
function conflictBlock(integ){return`【NUMERIC DATA CONFLICT — 審議停止】\n${integ.conflicts.map(x=>'・'+x).join('\n')}\n数値不一致が解消するまで選手評価・候補選定・最終判定を行わない。`;}
function installEvidence(){if(typeof window.searchDataEvidence!=='function'||window.MAGI_STRICT_EVIDENCE_WRAPPED===VERSION)return false;const prev=window.searchDataEvidence;window.searchDataEvidence=function(q){const e=prev(q);if(!e||!relevant(q))return e;const rows=((typeof dataRecords!=='undefined'?dataRecords:window.dataRecords)||[]).filter(r=>r&&r.source==='drive');const integ=build(rows);window.MAGI_NUMERIC_INTEGRITY=integ;window.MAGI_NUMERIC_FACTS=integ.facts;const clean=stripOldSnapshot(e.text);const block=integ.ok?trustedBlock(integ):conflictBlock(integ);e.text=block+'\n\n'+clean;e.numericIntegrity=integ;e.candidatePriority=block;e.summary=String(e.summary||'')+(integ.ok?' 数値整合性検証済み。':' 数値不一致のため審議停止。');return e};window.MAGI_STRICT_EVIDENCE_WRAPPED=VERSION;return true;}
function stopCard(q,integ){const status=document.getElementById('status');if(status)status.textContent='数値不一致を検出したため審議を停止しました。DATA CONFLICT';const question=document.getElementById('caseQuestion');if(question)question.textContent=q;const response=document.getElementById('response');if(response)response.classList.add('show');const title=document.querySelector('.final .title');if(title)title.textContent='《MAGI》数値検証停止';const verdict=document.getElementById('verdict');if(verdict)verdict.textContent='DATA CONFLICT';const reason=document.getElementById('reason');if(reason)reason.textContent=(integ?.conflicts||[]).join('／')||'数値の整合性を確認できません。';const next=document.getElementById('next');if(next)next.textContent='元データの不一致を解消してから再審議してください。';const protocol=document.getElementById('engineProtocol');if(protocol)protocol.innerHTML='';}
function installRunGate(){if(typeof window.runMagi!=='function'||window.MAGI_STRICT_RUN_WRAPPED===VERSION)return false;const original=window.runMagi;window.runMagi=async function(...args){const q=(document.getElementById('q')?.value||'').trim();if(relevant(q)){let e=null;try{e=window.searchDataEvidence(q)}catch(_){e=null}const integ=window.MAGI_NUMERIC_INTEGRITY;if(!e||!integ||!integ.ok){stopCard(q,integ||{conflicts:['数値検証処理を完了できない']});return;}}return original.apply(this,args)};window.MAGI_STRICT_RUN_WRAPPED=VERSION;return true;}
let tries=0;function boot(){tries++;const a=installEvidence(),b=installRunGate();if((!a&&!window.MAGI_STRICT_EVIDENCE_WRAPPED)||(!b&&!window.MAGI_STRICT_RUN_WRAPPED)){if(tries<200)setTimeout(boot,100);return}window.MAGI_NUMERIC_STRICT_GATE=VERSION;}
boot();
})();
