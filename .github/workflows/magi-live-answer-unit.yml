name: MAGI Live Answer Unit Selftest

on:
  push:
    branches: [main]
    paths:
      - 'server/api/magi/_live-answer.js'
      - 'server/api/magi/_strict-pitching-answer.js'
      - 'main-live-answer-v329.js'
      - 'magi-formal-runner-v358.js'
      - 'magi-app-bootstrap-v358.js'
      - 'engine-progress-v358.js'
      - 'index.html'
      - 'scripts/test-live-answer-routing.mjs'
      - 'scripts/test-formal-evidence-handoff-v358.mjs'
      - '.github/workflows/magi-live-answer-unit.yml'
  workflow_dispatch:

jobs:
  exact-answer-unit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --ignore-scripts --no-audit --no-fund

      - name: Run live answer unit cases
        run: node --experimental-default-type=module scripts/test-live-answer-routing.mjs

      - name: Run engine-boundary numeric Evidence and progress regression
        run: node scripts/test-formal-evidence-handoff-v358.mjs
