(()=>{
'use strict';
if(window.MAGI_MAIN_SEMANTIC_FIRST_V329)return;
window.MAGI_MAIN_SEMANTIC_FIRST_V329=true;
window.MAGI_MAIN_LIVE_ANSWER_V329=true;
window.MAGI_MAIN_LIVE_ANSWER_V326=true;
window.MAGI_MAIN_LIVE_ANSWER_V323=true;
window.MAGI_MAIN_LIVE_ANSWER_V318=true;

const $=id=>document.getElementById(id),txt=v=>String(v??'').trim();
const legacyRun=typeof window.runMagi==='function'?window.runMagi:null;
let busy=false;
const CONTEXT_KEY='magi-semantic-context-v329';
function mainButton(node){return node?.closest?.('button[onclick*="runMagi"]')||null}
function contextLoad(){try{const a=JSON.parse(sessionStorage.getItem(CONTEXT_KEY)||'[]');return Array.isArray(a)?a.slice(-10):[]}catch(_){return[]}}
function contextPush(role,text){text=txt(text);if(!text)return;try{const a=contextLoad();a.push({role,text:text.slice(0,1200)});sessionStorage.setItem(CONTEXT_KEY,JSON.stringify(a.slice(-10)))}catch(_){}}
function ensurePanel(){let p=$('magiLiveAnswerV329');if(p)return p;const judge=$('judge'),card=judge?.querySelector?.('.card');if(!judge||!card)return null;p=document.createElement('div');p.id='magiLiveAnswerV329';p.style.cssText='display:none;margin-top:12px;padding:16px;border:1px solid #2f5578;border-left:5px solid #37b579;border-radius:16px;background:rgba(10,27,46,.98);color:#f7f9fc;box-shadow:0 12px 30px rgba(0,0,0,.18)';p.innerHTML='<div style="font-size:11px;letter-spacing:.14em;color:#8db0d1;margin-bottom:8px">MAGI ANSWER</div><div id="magiUnderstoodV329" style="font-size:11px;line-height:1.55;color:#8db0d1;margin-bottom:8px"></div><div id="magiLiveAnswerTextV329" style="font-size:17px;font-weight:850;line-height:1.65"></div><div id="magiLiveAnswerMetaV329" style="margin-top:10px;font-size:11px;line-height:1.65;color:#a9bdd0"></div>';card.insertAdjacentElement('afterend',p);return p}
function hidePanel(){const p=$('magiLiveAnswerV329');if(p)p.style.display='none'}
function setRouter(title,badge,help){if($('routeValue'))$('routeValue').textContent=title;if($('routeBadge'))$('routeBadge').textContent=badge;if($('routeHelp'))$('routeHelp').textContent=help}
function showAnswer(result,totalMs){const p=ensurePanel();if(!p)return;const answer=txt(result?.answer||result?.clarificationQuestion)||'回答を取得できませんでした。',semantic=result?.semantic||{},understood=txt(semantic?.understoodRequest||result?.understoodRequest);$('magiUnderstoodV329').textContent=understood?`質問理解：${understood}`:'';$('magiLiveAnswerTextV329').textContent=answer;const bits=[];if(semantic?.mode)bits.push(`意図：${semantic.mode}`);if(result?.source?.name)bits.push(`参照：${result.source.name}`);if(result?.answerEngineVersion)bits.push(`回答：${result.answerEngineVersion}`);if(Number.isFinite(totalMs))bits.push(`処理：${(totalMs/1000).toFixed(1)}秒`);$('magiLiveAnswerMetaV329').textContent=bits.join(' ｜ ');$('response')?.classList.remove('show');p.style.display='block';if($('status'))$('status').textContent='MAGI 回答完了';setRouter('質問理解 → 検証回答','ACCURACY','質問の意味を先に確認し、その意図に合うデータ処理を実行しました。');p.scrollIntoView({behavior:'smooth',block:'center'});contextPush('assistant',`${understood?understood+'。':''}${answer}`)}
async function requestCore(question){const c=new AbortController(),t=setTimeout(()=>c.abort(),70000);try{const r=await fetch('/api/magi/core',{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'Content-Type':'application/json'},body:JSON.stringify({question,context:contextLoad()}),signal:c.signal});const d=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(txt(d?.error)||`CORE ${r.status}`),{status:r.status});return d}finally{clearTimeout(t)}}
function reportQuery(result,original){const s=result?.semantic||result||{},p=txt((s.players||result.players||[])[0]);let period='';switch(s.timeScope||result.timeScope){case'CAREER':period='通算';break;case'CURRENT_SEASON':period='現チーム';break;case'PREVIOUS_SEASON':period='旧チーム';break;case'SPECIFIC_SEASON':period=txt(s.specificSeason||result.specificSeason);break;case'RECENT_6':case'RECENT':period='直近';break;default:period='通算';}const kind=result.reportKind;if(kind==='PITCHING')return`${p}の${period}投手成績一覧を見せて`;if(kind==='FIELDING')return`${p}の${period}守備成績一覧を見せて`;if(kind==='BATTING')return`${p}の${period}打撃成績一覧を見せて`;return original}
async function waitFor(fnName,ms=6000){const start=Date.now();while(Date.now()-start<ms){if(typeof window[fnName]==='function')return window[fnName];await new Promise(r=>setTimeout(r,80))}return null}
async function runFullReport(result,original,btn){hidePanel();const kind=result.reportKind,q=$('q'),old=q?.value,synthetic=reportQuery(result,original);if(q)q.value=synthetic;setRouter(`質問理解 → ${kind==='BATTING'?'打撃':kind==='PITCHING'?'投手':'守備'}フルレポート`,'REPORT',`理解結果：${txt(result?.understoodRequest||result?.semantic?.understoodRequest)}`);if($('status'))$('status').textContent='質問の意図に合わせて正本データを集計しています…';try{if(kind==='BATTING'){const fn=await waitFor('MAGI_STATS_REPORT_FN');if(!fn)throw new Error('打撃レポート処理を準備できませんでした');await fn.call(window)}else if(kind==='PITCHING'){const fn=await waitFor('MAGI_PITCH_DIRECT_FN');if(!fn)throw new Error('投手レポート処理を準備できませんでした');await fn.call(window)}else if(kind==='FIELDING'){const fn=await waitFor('MAGI_RUN_FIELDING_REPORT');if(!fn)throw new Error('守備レポート処理を準備できませんでした');await fn(synthetic)}else throw new Error('対応するレポート種類がありません');contextPush('assistant',`${txt(result?.understoodRequest||result?.semantic?.understoodRequest)}。フルレポートを表示した。`)}finally{if(q&&old!==undefined)q.value=old;if(btn){btn.disabled=false;btn.textContent='MAGI実行'}}}
async function deliberate(result){if(!legacyRun)throw new Error('正式MAGI審議エンジンを呼び出せません');const packet=result?.evidencePacket||null,oldSearch=window.searchDataEvidence,oldRoute=window.routeQuestion;if(packet?.text)window.searchDataEvidence=()=>packet;window.routeQuestion=()=>({type:packet?.text?'data':'judge',label:'質問理解 → 証拠取得 → 3賢人審議',help:packet?.text?'質問の意味から必要資料を取得し、その内容を審議材料として渡します。':'質問内容を3賢人の正式審議へ渡します。'});setRouter('質問理解 → 3賢人審議','MAGI',txt(result?.understoodRequest)||'質問の意味を確定してから審議します。');if($('status'))$('status').textContent='質問の意味を確定。3賢人審議を開始します…';try{const out=await legacyRun.call(window);contextPush('assistant',`${txt(result?.understoodRequest)}について3賢人審議を実行した。`);return out}finally{if(oldSearch)window.searchDataEvidence=oldSearch;else try{delete window.searchDataEvidence}catch(_){}if(oldRoute)window.routeQuestion=oldRoute;else try{delete window.routeQuestion}catch(_){}}}
async function execute(btn){const q=txt($('q')?.value);if(!q){if($('status'))$('status').textContent='相談内容を入力してください。';return}if(busy)return;busy=true;const started=performance.now(),oldText=btn?.textContent;contextPush('user',q);try{hidePanel();if(btn){btn.disabled=true;btn.textContent='質問を理解中…'}setRouter('質問を理解しています','THINK','単語一致ではなく、質問全体の意味・対象・期間・目的を確認しています。');if($('status'))$('status').textContent='質問内容を理解しています。正確性を優先するため少し時間がかかる場合があります…';const r=await requestCore(q);if(r?.action==='FULL_REPORT')return await runFullReport(r,q,btn);if(r?.action==='DELIBERATE'||r?.route==='DELIBERATION')return await deliberate(r);if(r?.action==='CLARIFY'||r?.route==='CLARIFY'){showAnswer(r,performance.now()-started);return}if(r?.handled!==false){showAnswer(r,performance.now()-started);return}if(!legacyRun)throw new Error('既存処理へ引き継げません');setRouter('質問理解 → 既存処理','LEGACY','質問理解後、既存の対応処理へ引き継ぎます。');return await legacyRun.call(window)}catch(e){console.warn('[MAGI semantic first v329]',e?.message||e);if(e?.status===401){if($('status'))$('status').textContent='LINEログインを確認してください。'}else if(e?.status===403){if($('status'))$('status').textContent='利用承認を確認してください。'}else{if($('status'))$('status').textContent='質問理解またはデータ処理を完了できませんでした。推測回答はせず、もう一度実行してください。'}setRouter('処理未完了','RETRY','正確性を優先し、意味理解に失敗した場合は推測回答を出しません。')}finally{if(btn){btn.disabled=false;if(/質問を理解中/.test(btn.textContent||''))btn.textContent=oldText||'MAGI実行'}busy=false}}

document.addEventListener('click',event=>{const btn=mainButton(event.target);if(!btn)return;event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();execute(btn)},true);
const observer=new MutationObserver(()=>{if($('judge'))ensurePanel()});observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ensurePanel,{once:true});else ensurePanel();
})();
