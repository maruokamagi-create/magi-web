# MAGI Web Recovery Snapshot — 2026-08-27

## Protected baseline
- Branch: `recovery-20260827`
- Baseline commit: `e7db4f13aa70abbb4db89af280d3cf74c5be3c46`
- Baseline state: latest committed `main` state at recovery start.

## Current entry point
- `index.html`
- Page title: `MAGI Web v0.10.52`
- Loads:
  - `/loader-v130.js?v=182`
  - `/persona-png-fix-v170.js?v=182`
  - `/flow-image-final-v182.js?v=187`

## Confirmed preserved assets
- `assets/magi-flow-transparent.png`
- `portraits/melchior.png`
- `portraits/balthasar.png`
- `portraits/casper.png`
- `magi-official-symbol-v125.svg`
- `index-legacy-v092.html`

## Recovery evidence outside GitHub
ChatGPT Library contains:
- `MAGIの仕組み_確定版_20260826.jpeg` — approved four-step MAGI workflow artwork.
- `index_v180.html` — MAGI Web v0.10.50 entry point using `flow-image-v180.js`.

## Late-session evidence discovered 2026-08-27
The user continued testing well after the 10:34 JST GitHub commit. Library screenshots from roughly 20:44–23:55 JST on 2026-08-26 show:
- Vercel production project `magi-web` remained live at `magi-web.vercel.app`.
- Multiple Vercel projects existed, including `magi-web`, `magi-web-test`, `magi-png-transfer-test`, and a recent `magi-engine-v1-development` item visible in the account dashboard.
- The user inspected Vercel project settings and was shown a Pro upsell, but the team dashboard shown later was still being used for the existing `magi-web` production deployment.
- The live MAGI UI was being tested around Google Drive OAuth, account selection/memory, disconnect/reconnect, folder rescan, and automatic indexing.
- A successful state showed Google Drive connected with 49 items, 23 files, 6 indexed files and 565 indexed rows.
- The final Library screenshot from 23:55 JST still showed the `magi-web` Vercel project card; there are no later 2026-08-26 Library captures after that screenshot in the recovered listing.

## Important source recovery
The Google Drive/OAuth work is not lost: the implementation is present inside `index-legacy-v092.html`, including:
- read-only Google Drive OAuth
- Google account hint stored locally for next connection
- account-change / forget flow
- disconnect flow
- recursive team-folder scan with fallback indexing
- automatic indexing for Sheets, Excel, CSV, JSON, TXT and Google Docs
- images/PDFs listed only
- local CSV/JSON/Excel import

## Late hero copy confirmed preserved
Library evidence records a late user request to:
- keep the introductory sentence `データ・戦術・人の3視点で審議し、必要な時はGoogle Driveのチーム資料を読み取り専用で参照します。` visually as one tighter paragraph
- change the main Japanese hero copy to `丸岡中野球部の今と未来を支える、3つの人格による戦術解析・意思決定支援システム`

This is already preserved in current `loader-v130.js`:
- `brand()` writes that exact `今と未来を支える` hero copy
- matching intro paragraphs receive `.magiIntroRef`
- `.magiIntroRef` uses tighter line-height (`1.2`)

Therefore this late visual/copy adjustment does not need to be recreated.

## Vercel connector discrepancy
The recovered screenshots clearly show the Vercel team `maruokamagi-2208's projects` with `magi-web` and related projects. The currently connected Vercel API connector identifies the same team as Hobby but returns an empty project list / 404 for `magi-web`. Treat this as a connector/account visibility discrepancy, not evidence that the historical project was deleted.

## Investigation: `magi-engine-v1-development`
Recovery checks performed on 2026-08-27:
- Library search confirms `magi-engine-v1-development` appears on the Vercel dashboard in the late 2026-08-26 session.
- The currently connected Vercel API cannot fetch that project by slug and returns 404.
- GitHub account access currently exposes only repository `maruokamagi-create/magi-web`; no separate MAGI ENGINE repository is visible.
- Code search inside `magi-web` found no separate server/API implementation named `magi-engine-v1-development`.
- Existing MAGI architecture documents describe `Web版MAGI` as the user-facing entrance, `ChatGPT / MAGI ENGINE` as the analysis/decision “brain,” and Google Drive as storage/memory. This means the phrase MAGI ENGINE already existed conceptually before the late Vercel project appeared.

### Current conclusion about the engine project
There is evidence that a Vercel item named `magi-engine-v1-development` existed, but there is currently **no recovered evidence that it contained unique source code that replaced or superseded `magi-web`**. It may have been an experimental/development deployment created during Work/Codex activity. Because its source repository, deployment contents, and final state are not recoverable through the currently connected Vercel/GitHub APIs, treat it as **unverified experimental state**, not as the authoritative MAGI baseline.

This narrows the safe recovery target to the committed `magi-web` code plus the late-session screenshots and documents already recovered.

## Recovery interpretation
The latest visible user work was NOT only the MAGI flow-image integration. The later session included:
1. hero copy/line-spacing refinement — confirmed already committed in `loader-v130.js`
2. Vercel project/settings inspection — screenshots preserved
3. Google Drive OAuth/account-memory testing — source preserved in `index-legacy-v092.html`
4. successful Drive indexing — confirmed by screenshot (49 items / 23 files / 6 files / 565 rows)
5. appearance of an experimental `magi-engine-v1-development` Vercel item — existence confirmed, unique source state not recovered

No recovered evidence currently proves that an additional authoritative MAGI source-code change after these steps was successfully saved outside GitHub. Continue treating any Work/Codex-only edits after the final saved GitHub state as unverified until additional evidence is found.

## Rule
Do not delete or rewrite this recovery branch while recovery is in progress. Do not alter `main` until the late-session state has been fully reconstructed and compared against screenshots.
